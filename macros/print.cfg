[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default(0)|float %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  MESSAGE MSG="Print Starting: Hotend:{target_extruder}c, Bed: {target_bed}c, Chamber: {target_chamber}c" 
  
  _SET_MPC_MATERIAL MATERIAL={params.MATERIAL}

  G90                                          # Absolute position
  {% if target_chamber > 0 %}
    M140 S{[target_bed+10, 125]|min}               # boost bed temp to help heat chamber
    FILTER_ON                                      # Turn on the filter
    SET_CHAMBER CHAMBER_TEMP={target_chamber}
  {% else %}
    M140 S{target_bed}                                  # Set the target temp for the bed
  {% endif %}

  NOZZLE_LED
  G28                                                   # Full home (XYZ)
  BED_MESH_CLEAR                                        # Clear old saved bed mesh (if any)
  PARK_CENTER                                                  # Go to center of the bed

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak and toggle filter.
  {% if params.BED|int > 90 %}
    M106 S255                                           # Turn on the part cooling fan
    M140 S{[target_bed+15, 125]|min}
    HEATSOAK_CHAMBER CHAMBER_TEMP={target_chamber} 
  {% endif %}
 
  M140 S{target_bed}                                               # Set the target temp for the bed

  # cleaning
  PARK
  MESSAGE MSG="Heating for cleaning: {target_extruder * 0.75}"    # Display info on display
  M109 S{target_extruder * 0.75}                                  # Heat hotend for cleaning
  NOZZLE_CLEAN
  M104 S150                                                        # Start z offset nozzle temp
  PARK

  MESSAGE MSG="Waiting for bed heating: {target_bed}c"             # Display info on display
  M190 S{target_bed}                                               # Wait for bed to reach target temp

  MESSAGE MSG="Heating for z offset: 150c"                       # Display info on display
  M109 S150                                                      # Heat hotend to 150c. This helps with getting a correct Z-home.
  MESSAGE MSG="Beacon Calibration"                               # Display info on display
  BEACON_AUTO_CALIBRATE                                          # Calibrate Beacon probe once bed to temp & nozzle preheated
  M104 S0

  MESSAGE MSG="Leveling"                               # Display info on display
  QUAD_GANTRY_LEVEL                                    # Level the printer via QGL
  G28 Z                                                # Home Z again after QGL

  MESSAGE MSG="Bed Mesh"
  BED_MESH_CALIBRATE ADAPTIVE=1                                   # Start the bed mesh (add ADAPTIVE=1) for adaptive bed mesh

  # Heat up the hotend up to target via data from slicer
  MESSAGE MSG="Final heating for print: {target_extruder}c"     # Display info on display
  M107                                                  # Turn off partcooling fan
  PARK                                                  # Go to bucket catch area
  M109 S{target_extruder}                               # Heat the hotend to set temp
  NOZZLE_CLEAN WIPES=2                                  # Quick clean to knock off ooze

  MESSAGE MSG="Printing..."               # Display info on display
  SKEW_PROFILE LOAD=Calilantern

  # set nozzle thermal expansion offset
  {% if printer.configfile.settings.beacon is defined %}
      _BEACON_SET_NOZZLE_TEMP_OFFSET 
  {% endif %}
  
  LINE_PURGE

[gcode_macro PRINT_END]
#   Use PRINT_END for the slicer ending script - please customise for your slicer of choice
gcode:
    {% set th = printer.toolhead %}
    SET_SKEW CLEAR=1
    # reset nozzle thermal expansion offset
    {% if printer.configfile.settings.beacon is defined %}
        _BEACON_REMOVE_NOZZLE_TEMP_OFFSET
        _BEACON_SET_NOZZLE_TEMP_OFFSET RESET=True
    {% endif %}

    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-10.0 F3600                ; retract filament

    G91                            ; relative positioning
    G0 Z1.00 X20.0 Y20.0 F20000    ; move nozzle to remove stringing
    TURN_OFF_HEATERS
    M107                           ; turn off fan
    G1 Z2 F3000                    ; move nozzle up 2mm
    G90                            ; absolute positioning
    G0  X{th.axis_maximum.x//2} Y{th.axis_maximum.y} F3600            ; park nozzle at rear
    NOZZLE_LED S=0                  ; turn off nozzle LED
    BED_MESH_CLEAR
    MESSAGE MSG="Print Done!"          ; Display info on display
    UPDATE_DELAYED_GCODE ID=filter_off DURATION=180